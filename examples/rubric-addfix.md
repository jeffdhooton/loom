Grade the git diff of a fix to examples/sandbox/calc.py.

Criteria:
1. The diff modifies the `add` function so it returns the SUM of its two
   arguments (`a + b`), replacing the previous subtraction (`a - b`).
2. No unrelated files or functions are changed.

Pass (score >= 0.8) only if criterion 1 is clearly satisfied by the diff.
